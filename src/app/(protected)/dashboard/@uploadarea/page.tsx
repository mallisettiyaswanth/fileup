import { Card } from "@/components/ui/card";
import React from "react";

type Props = {};

const UploadArea = (props: Props) => {
  return <Card className="flex-1">UploadArea</Card>;
};

export default UploadArea;
